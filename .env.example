# CrimRXiv Archive - Environment Configuration
# Copy this file to .env and configure for your environment

# ==================================================
# ARWEAVE WALLET
# ==================================================

# Path to your Arweave wallet JSON file
ARWEAVE_WALLET_PATH=/path/to/your/wallet.json

# ==================================================
# PUBPUB CREDENTIALS (for importing articles)
# ==================================================

PUBPUB_EMAIL=your-email@example.com
PUBPUB_PASSWORD=your-password

# ==================================================
# ARNS CONFIGURATION (for data deployment)
# ==================================================

# Root ArNS name (e.g., crimrxiv → crimrxiv.arweave.net)
ARNS_ROOT_NAME=crimrxiv

# Data undername (e.g., data → data_crimrxiv.arweave.net/metadata.parquet)
ARNS_DATA_UNDERNAME=data

# WASM undername (e.g., wasm → wasm_crimrxiv.arweave.net/duckdb-mvp.wasm)
ARNS_WASM_NAME=wasm

# ANT Process ID (get this from https://arns.app after purchasing name)
# REQUIRED for automatic ArNS updates when uploading parquet/wasm
ARNS_PROCESS_ID=your-ant-process-id-here