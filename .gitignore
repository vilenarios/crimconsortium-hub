# CrimConsortium Hub - Git Ignore Configuration
# SPA Architecture (Vite + DuckDB-WASM)

# Environment and secrets
.env
.env.local
.env.production
.env.development
*.key
*.pem
wallet.json
*.wallet
secrets/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
.npm
.yarn-integrity

# Build outputs (Vite SPA)
dist/
build/
.cache/

# Temporary files
data/temp/
data/state/
cache/
*.tmp
*.log

# Data management - Generated files (run `npm run import` then `npm run export`)
# OLD static site data (no longer used)
data/final/

# NEW SPA data pipeline
data/temp/
data/state/
data/manifests/

# SQLite databases (generated by scrapers)
data/sqlite/
*.db
*.db-journal
*.db-wal
*.db-shm

# Parquet files (exported from SQLite, served to browser by Vite)
# NOTE: public/data/ contains Parquet files that ARE committed (required for SPA)
data/parquet/
*.parquet
*.arrow

# Export artifacts
export/
PUB-PUB-API.json

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Operating system files
*.pid
*.seed
*.pid.lock

# Coverage and testing
coverage/
.nyc_output
.coverage

# Deployment artifacts (may contain sensitive data)
deployment-state.json
arns-configuration.json
upload-state.json

# Debug and test files
debug-*.json
test-*.json
test-*.js
validation-*.json
verify-*.js

# Large export files (keep structure but not content for repo)
# Note: Keep export structure in repo but not the large files
# Team should add their own export data locally

# ArDrive cache files
.ardrive/
ardrive-cache/

# Backup files
*.backup
*.bak

# Logs
logs/
*.log

# Runtime files
pids/
*.pid